# Address of the running GuestPortal Connect API.
GPC_URL=http://localhost:3100

# The platform key, from `npm run seed:platform` in guestportal-connect.
# Server-side only: these are read by the Next.js route handler and are never
# sent to the browser.
PLATFORM_CLIENT_ID=
PLATFORM_CLIENT_SECRET=
